/// <reference types="react" />
import { ModalVersion, WalletModalProps } from '@cosmos-kit/core';
export declare const getModalFromVersion: (version: ModalVersion) => ({ isOpen, setOpen }: WalletModalProps) => JSX.Element;
