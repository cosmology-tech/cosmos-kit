import { ModalVersion, WalletModalProps } from '@cosmos-kit/core';
export declare const getModal: (version: ModalVersion) => ({ isOpen, setOpen, walletRepo, theme }: WalletModalProps) => JSX.Element;
