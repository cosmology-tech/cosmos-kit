import { WalletModalProps } from '@cosmos-kit/core';
export declare function getWalletModal(theme?: Record<string, any>): ({ isOpen, setOpen, walletRepo }: WalletModalProps) => JSX.Element;
export declare const DefautModal: ({ isOpen, setOpen, walletRepo }: WalletModalProps) => JSX.Element;
